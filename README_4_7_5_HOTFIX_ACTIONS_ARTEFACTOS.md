# COLIBRÍ ERP 4.7.5 · Hotfix GitHub Actions

## Correcciones

- Sustituida la referencia obsoleta `totalCobrados` por `totalCabecerasResultado` en `sync/ColibriSync/Program.cs`.
- Corregido el workflow Clean para copiar `config.example.json` como `config.json`.
- Actualizada la versión del ejecutable a 4.7.5.

Tras subir este proyecto a `develop`, los workflows .NET deben compilar y publicar sus artefactos.
